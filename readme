1) изначально конфиг использовал yaml формата

2) на этапе установки появились проблемы с зависимостями (PyYAML.x86_64), 
поэтому переделал конфиг на json
установку проверял на докер образе centos:7

3) не знаю, правильно ли понял задачу, но сделал как понял :)
для проверки изпользовал VPS с Сentos 7
в host_vars нужно закомментить confmsg для проверки подстановки default_vars
так же последней таской удаляю пакет из системы

4) сделал 2 варианта докерфайла, с простым копированием файлов и с установкой RPM
